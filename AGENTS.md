# Repository instructions

## Git history and tags

- Do not create commits unless the user explicitly requests a commit.
- Do not create, delete, move, or force-update Git tags unless the user
  explicitly requests that exact tag operation.
- Never perform either operation silently. Before executing an explicitly
  requested commit or tag operation, state the intended commit or tag action
  in the user-facing update.
